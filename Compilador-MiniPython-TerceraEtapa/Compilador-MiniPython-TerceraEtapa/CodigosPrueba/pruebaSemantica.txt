# Código con errores de variables y parámetros

def factorial(n):
    if n <= 1:
        return 1
    else:
        return n * factorial(n - 1)

x = 10
y = undefined_var   # Error: variable no definida

def greet(name, greeting):
    print(greeting + ", " + name)

greet("World")  # Error: cantidad incorrecta de parámetros
factorial(x)    # Esta llamada debería estar bien
undefined_func()  # Error: función no definida
